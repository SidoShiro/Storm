#include <stdio.h>  /* Affichage */
#include <stdlib.h> 

/* main contient le programme */
int main(int argc, char* argv[])
{
    /* Entrée du programme */

    /* Initialisation des variables */
	int a = 2;
    int b = 3;
    int c = a + b;

    /* Affichage de c le résultat */
    printf("%d", c);

    /* Sortie du Porgramme */
	return EXIT_SUCCESS;
}
