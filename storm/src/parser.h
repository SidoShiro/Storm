# ifndef _PARSER_H_
# define _PARSER_H_

# include <stdio.h>
# include <stdlib.h>

// Prototypes
void parseParam(int count, char* para[]);
int strpara(char* str1, char* str2);
void print(char* str);

# endif

